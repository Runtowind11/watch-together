# 情侣异地同步观影平台

## 项目简介

为异地情侣提供 Web 端的实时同步观影体验。一方通过本地电脑播放视频，另一方通过浏览器实时同步观看播放状态（播放、暂停、拖动、倍速）。通过 Tailscale 组建虚拟局域网，无需公网 IP 和云服务器。

## 核心需求

| # | 需求 | 说明 |
|---|---|---|
| 1 | 异地同步播放 | 两人异地，播放状态实时同步 |
| 2 | 本地视频播放 | 服务器端读取本地视频文件，通过 HTTP 流式传输 |
| 3 | 网络 URL 播放 | 同时也支持直接输入视频 URL（如网盘直链、m3u8） |
| 4 | Tailscale 组网 | 双方设备安装 Tailscale，虚拟局域网直连，数据端到端加密 |
| 5 | 跨端适配 | 同一套 Web App 自适应 PC 和移动端 |
| 6 | 房间机制 | 创建房间 → 分享链接/房间码 → 另一方加入，无需注册 |
| 7 | 主控模式 | 上传者为主控，拥有播放控制权，跟随者仅观看，可切换角色 |
| 8 | 集成现有站点 | 独立项目开发，完成后通过导航链接集成到现有情侣站点 |

## 技术栈

| 层 | 技术 | 选型理由 |
|---|---|---|
| **前端框架** | React + TypeScript + Vite | 构建快、类型安全 |
| **UI** | CSS Modules + 响应式布局 | 轻量无额外依赖，一套代码适配 PC/Mobile |
| **播放器** | Plyr | 移动端触摸友好，API 完善 |
| **实时通信** | Socket.io + Room | 自动重连、自带房间机制 |
| **后端** | Node.js + Express | 同一语言栈，轻量高效 |
| **网络组网** | Tailscale（WireGuard） | 免费、端到端加密、无需公网 IP |
| **视频存储** | 服务器本地磁盘 | 视频文件存在跑服务的那台电脑上 |
| **SSL/HTTPS** | Tailscale 自带或 Cloudflare | 传输加密 |

## 项目结构

```
couple-movie/
├── client/                        # React 前端
│   ├── index.html
│   ├── vite.config.ts
│   ├── package.json
│   └── src/
│       ├── main.tsx
│       ├── App.tsx
│       ├── socket.ts              # Socket.io 客户端单例
│       ├── pages/
│       │   ├── Home.tsx           # 创建/加入房间
│       │   └── WatchRoom.tsx      # 观影房间（播放器 + 同步）
│       ├── components/
│       │   ├── VideoPlayer.tsx    # Plyr 封装 + 同步事件绑定
│       │   ├── UrlInput.tsx       # 输入视频 URL
│       │   ├── SyncStatus.tsx     # 连接状态指示器
│       │   └── RoleSwitch.tsx     # 角色切换按钮
│       ├── hooks/
│       │   ├── useSync.ts         # 同步逻辑封装
│       │   └── useRoom.ts         # 房间生命周期管理
│       └── styles/
│           ├── global.css         # 全局样式
│           └── responsive.css     # PC/Mobile 响应式断点
│
├── server/                        # Express 后端
│   ├── package.json
│   ├── tsconfig.json
│   └── src/
│       ├── index.ts               # Express + Socket.io 入口
│       ├── socket/
│       │   └── sync.ts            # Socket.io 同步事件处理
│       ├── routes/
│       │   └── video.ts           # 本地视频文件流式服务
│       └── config.ts              # 配置（端口、视频路径）
│
└── README.md
```

## 构建计划

### Phase 1 — 基础骨架 + 实时同步

- 初始化 React + Vite 前端项目
- 初始化 Express + Socket.io 后端项目
- 实现房间创建/加入（socket room）
- 实现 `useSync` hook：play / pause / seek / rate 事件双向同步
- 集成 Plyr 播放器，用测试视频 URL 验证同步效果

### Phase 2 — 本地视频流式播放 + URL 输入

- 后端 `/api/video/local` 接口：读取本地视频文件，支持 HTTP Range 请求（Seek 支持）
- 前端 URL 输入组件：直接输入网络视频地址开始播放
- 前端加入同步状态提示（已连接/已同步/延迟 ms）

### Phase 3 — 跨端适配

- 响应式 CSS 断点（768px）
- 移动端全屏播放 + 手势控制（Plyr 内置）
- 移动端触摸优化的控制栏布局
- 移动端网络断开自动重连 + 同步恢复

### Phase 4 — 体验优化

- 角色切换功能（主控 ↔ 跟随者）
- 同步校准算法（每 5s 对时，消除网络延迟累积偏差）
- Socket.io 重连后自动恢复播放位置
- 错误处理与用户提示

### Phase 5 — Tailscale 组网 + 集成上线

- 编写 Tailscale 安装与配置指南（Windows + 手机端）
- 测试 Tailscale 虚拟局域网下的同步效果
- 在现有情侣站点导航栏添加「一起观影」入口链接
- 提供启动脚本（一键启动前后端）
```
